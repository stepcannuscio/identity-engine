[flake8]
max-line-length = 100
exclude = .venv,__pycache__
per-file-ignores =
    # sys.path.insert() must precede project imports in scripts and tests;
    # E402 (module-level import not at top) is intentional in these files.
    scripts/*.py:E402
    tests/*.py:E402
